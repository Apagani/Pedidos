<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="youtube.demo.youtubedemo"
    android:name=".ClassGlobal">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/white"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity
            android:name="pedido.Activitys.MainActivity"
            android:label="Importación Celulares"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden">
        </activity>
        <activity
            android:name="pedido.Activitys.detalle_productoActivity"
            android:label="Producto"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name="pedido.Comunicacion.comunicaciondatos"
            android:label="Comunicando"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Transparent">
        </activity>
        <activity
            android:name="pedido.Activitys.Pedido_enviado"
            android:label="Compra Realizada"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name="pedido.Activitys.ConexionActivity"
            android:label="Configuracion"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name="pedido.Activitys.LoginActivity"
            android:label="Importación Celulares"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>